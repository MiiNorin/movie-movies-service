server:
  port: 8082   # (user-service), 8082 (movie-service)

spring:
  application:
    name: movie-service   # movie-service tương tự

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka
    register-with-eureka: true
    fetch-registry: true
